<div class="col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-1 main profile">
  {{> flash}}

  <div class="row page-header">
    <img src="{{user.profile.picture}}" class="profile-pic" alt="{{user.profile.displayName}} | Pizzafy" />  
    <h1 class="page-title">{{user.profile.displayName}}'s Pizza Profile</h1> 
    <!-- Not using this for now <button type="button" class="badge pizza-number" data-toggle="tooltip" data-placement="top" title="Your Pizza Number: How many slices of pizza you eat">2</button> -->
  </div>
  <a href="#" class="edit_profile-pic"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Update your profile picture</a>

  <!-- Note: Profile picture requirements: Must be able to crop to square -->

  <!-- Need to be able to replace your photo here -->

  <form action="" method="POST" id="profileForm" class="form-horizontal">
    <input type="hidden" name="_csrf" value="{{_csrf}}">
    <h3>Basics</h3>
    <p>Tell us a little more about you, so we can better optimize your pizza experience!</p>

    <div class="first-name row">
        <div class="control-group">
          <label class="control-label group-title" for="hates">First Name</label>
          <div class="controls">
            <input id="" name="" type="text" placeholder="" class="input-xlarge">
          </div>
        </div>
      </div>
      <div class="last-name row">
        <div class="control-group">
          <label class="control-label group-title" for="hates">Last Name</label>
          <div class="controls">
            <input id="" name="" type="text" placeholder="" class="input-xlarge">
          </div>
        </div>
      </div>

    <fieldset>
      <h3>Pizza Number</h3>
      <p>How many slices of pizza do you eat at the typical pizza party?</p>
      <div class="pizza-number-group row">
        <div class="control-group">
          <label class="control-label" for="pizzaNumber">Slices</label>
          <div class="controls">
            <input id="pizzaNumber" name="pizzaNumber" type="text" class="input-mini" value="{{user.pizza.pizzaNumber}}" required="">
          </div>
        </div>
      </div>

      <!-- Toppings Love -->
      <!-- This needs to be reorganized majorly, by type of topping -->
      <h3>Toppings</h3>
      <p>Tell us about the toppings you love and hate!</p>
      <div class="toppings-love row">
        <div class="control-group">
          <label class="control-label group-title" for="checkboxes">Loves</label>
          <div class="controls">

            {{#each toppings}}
            <label class="checkbox inline" for="checkboxes-{{@index}}">
              <input type="checkbox" name="toppings" id="checkboxes-{{@index}}" value="{{this.name}}" class="toppings-item" {{#if this.checked}}checked="{{this.checked}}"{{/if}}>
              {{this.name}}
            </label>
            {{/each}}

          </div>
        </div>
      </div>

      <!-- Toppings Hate -->
      <!-- Predictive text tokens based on the same above inputs -->
      <div class="toppings-hate row">
        <div class="control-group">
          <label class="control-label group-title" for="hates">Hates</label>
          <div class="controls">
            <input id="hates" name="hates" type="text" placeholder="" class="input-xlarge">
            <p class="help-block">Which toppings will you not eat, no matter what?</p>
          </div>
        </div>
      </div>

      <!-- Crust preferences -->
      <!-- Multiple Radios (inline) -->
      <h3>Crust</h3>
      <p>Bread evanglist? Got strong opinions on the thickness of your dough? Sound off here!</p>
      <div class="crust-prefs row">
        <div class="control-group">
          <div class="controls">

            {{#each crusts}}
            <label class="radio inline" for="radios-{{@index}}">
              <input type="radio" name="crust" id="radios-{{@index}}" value="{{this.name}}" {{#if this.checked}}checked="{{this.checked}}"{{/if}}>
              {{this.name}}
            </label>
            {{/each}}

          </div>
        </div>
      </div>

      <!-- Dietary restrictions -->
      <h3>Dietary Restrictions</h3>
      <p>Got dietary restrictions? We got you covered!</p>
      <div class="dietary row">
        <div class="control-group">
          <div class="controls">

            {{#each dietary}}
            <label class="checkbox" for="dietary-{{@index}}">
              <input type="checkbox" name="dietary" id="dietary-{{@index}}" value="{{this.name}}" class="dietary-item" {{#if this.checked}}checked="{{this.checked}}"{{/if}}>
              {{this.name}}
            </label>
            {{/each}}

          </div>
        </div>
      </div>

      <!-- Notes -->
      <h3>Notes</h3>
      <p>Do you have any additional comments about your pizza preferences that you would want the organizer of any party to attend to know? Allergies? Requests? Something we didn't cover here?</p>
      <div class="notes row">
        <div class="control-group">
          <label class="control-label" for="notes"></label>
          <div class="controls">                     
            <textarea id="notes" name="notes">{{notes}}</textarea>
          </div>
        </div>
      </div>

      <!-- submit -->
      <div class="control-group submit-div">
        <label class="control-label" for="submit"></label>
        <div class="controls">
          <button id="profileSubmit" name="submit" class="btn btn-primary">Save changes</button>
        </div>
      </div>

    </fieldset>
  </form>

</div> <!--cols main-->
